export enum CLIENT_ROUTES {
  MAIN = "/",
  SIGN_IN = "/signin",
  SIGN_UP = "/signup",
  GAME = "/game",
  GAME_TEST = "/game-test",
  LOBBY_LIST = "/lobbies",
  SHOP = "/shop",
  PROFILE = "/profile"
}

export enum SOCKET_DRAW_ROUTES {
  DRAW = "draw",
  FINISH = "finish",
  CLEAR = "clear",
}
